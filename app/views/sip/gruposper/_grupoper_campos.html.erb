<div class='campos_grupoper'>
  <%= f.input :id, as: :hidden %>
  <% ph = 'N' %>
  <% ac = {'data-autocompleta' => 'no' } %>
  <% if (!f.object.nombre || f.object.nombre == 'N' || 
         f.object.nombre == '') %>
    <% ph = 'Buscar' %>
    <% ac = { } %>
  <% end %>
  <%= f.input :nombre, placeholder: ph,
    value_method: lambda {|t| '' if t.nombre == 'N'},
    :input_html => ac %>
  <%= fvictimacolectiva.simple_fields_for :orgsocial, 
    html: { :class => 'form-horizontal' } do |a| %>
    <%= a.input :id, as: :hidden %>
    <%= a.input :grupoper_id, as: :hidden %>

    <div class="row">
      <div class="col-md-6">
        <%= fvictimacolectiva.input :personasaprox %>
      </div>
      <div class="col-md-6">
        <%= a.input :fechafundacion,
          input_html: {'data-behaviour' => 'datepicker'},
          as: :string %>
      </div>
    </div>

    <%= render partial: 'sip/gruposper/grupoper_complemento1', locals: {f: f} %>

    <%= render partial: 'sivel2_gen/casos/lista_victimas_orgsocial', 
      locals: {a: a} %>

  <% end %>
</div>

